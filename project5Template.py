from random import randrange

# portNumArray should contain the port numbers
# portNameArray should contain the protocol abbreviations (such as HTTP, NetBIOS, SMB, etc.)

def numToName(portNumArray, portNameArray, portNumber):
    # Find portNumber in the portNumArray and return the name from the portNameArray.


def nameToNum(portNumArray, portNameArray, portName):
    # Find portName in portNameArray to return the port number in the portNumArray.

def main():


if(__name__=="__main__"):
    main()
